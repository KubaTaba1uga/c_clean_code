/* Converting one objects type into */
/*         another is called type casting. */
#include <stdio.h>

int main(void) {

  float a = 1.999999;

  float b = (int)a;

  printf("Before: %f\n After: %f\n", a, b);
}
