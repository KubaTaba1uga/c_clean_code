#include <stdio.h>

/* `volatile` prevents variable from being cached.
        It is especially usefull when it represent data
        from code that You don't have controll on.
        Like sensors, clocks or another threads.*/

int main(void) {}
